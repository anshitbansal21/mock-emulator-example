{
  "name": "mock-vs-emulate-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:traditional": "vitest run tests/traditional.mocking.test.js",
    "test:emulate": "vitest run tests/emulate.stateful.test.js"
  },
  "devDependencies": {
    "emulate": "latest",
    "vitest": "latest"
  }
}
